/* Holo Money — one responsive column: the phone layout IS the desktop carriage layout.
   Full-page desktop centers the column over the wallpaper; ?panel=1 and mobile go full-bleed.
   Tokens: hm-* are the app's Revolut-grammar candidates (PARITY-MATRIX §8.1–8.2) — the OS theme
   can override via --holo-money-*; defaults are the researched values. */

:root {
  color-scheme: dark;
  --hm-bg: var(--holo-money-bg, #191c1f);            /* Shark — matrix §8.1 */
  --hm-accent: var(--holo-money-accent, #7f84f6);    /* Cornflower Blue — matrix §8.2 */
  --bg: var(--hm-bg);
  --panel: var(--holo-surface, #212529);
  --panel2: var(--holo-surface-2, #282d33);
  --line: var(--holo-border, #33383e);
  --ink: var(--holo-ink, #e9edef);
  --dim: var(--holo-ink-dim, #8b95a1);
  --accent: var(--hm-accent);
  --ok: var(--holo-ok, #00d95f);
  --bad: var(--holo-danger, #e76f6f);
  --r: var(--holo-radius, 16px);
  --col: 480px;
  --sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  --mono: ui-monospace, "Cascadia Code", "SF Mono", Menlo, Consolas, monospace;
}
* { box-sizing: border-box; }
html, body { height: 100%; margin: 0; }
body { background: var(--bg); color: var(--ink); font: 400 16px/1.45 var(--sans); overflow: hidden; }

#app { position: relative; height: 100%; max-width: var(--col); margin: 0 auto;
  display: flex; flex-direction: column; background: var(--bg); }
body.page #app { border-left: 1px solid var(--line); border-right: 1px solid var(--line); }
body.panel #app, body.mobile #app { max-width: none; border: 0; }

/* ── screens ─────────────────────────────────────────────────────── */
.screen { position: absolute; inset: 0 0 64px 0; overflow-y: auto; overscroll-behavior: contain;
  opacity: 0; pointer-events: none; transform: translateY(6px); transition: opacity .22s ease, transform .22s ease; }
.screen.on { opacity: 1; pointer-events: auto; transform: none; }
.scr-hd { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 18px 18px 6px; }
.scr-hd h1 { margin: 0; font: 700 22px/1 var(--sans); letter-spacing: -.01em; }
.scr-hd .back { width: 34px; height: 34px; border: 0; border-radius: 50%; background: var(--panel);
  color: var(--ink); font: 600 20px/1 var(--sans); cursor: pointer; }
.scr-hd .hd-sp { width: 34px; }
.scr-hd .hd-actions { display: flex; align-items: center; gap: 8px; }

/* ── hero (Home) ─────────────────────────────────────────────────── */
.hero { position: relative; padding: max(10px, env(safe-area-inset-top)) 16px 18px;
  background:
    radial-gradient(140% 90% at 50% -18%, color-mix(in srgb, var(--hm-accent) 62%, #2a1d8f) 0%, rgba(24,18,70,.72) 46%, rgba(12,12,20,0) 78%),
    linear-gradient(180deg, #0d0e14 0%, var(--bg) 92%);
  border-bottom: 1px solid color-mix(in srgb, var(--line) 60%, transparent); }
.hero-bar { display: flex; align-items: center; gap: 10px; }
.avatar { width: 34px; height: 34px; border-radius: 50%; border: 0; cursor: pointer; flex: 0 0 auto;
  background: linear-gradient(135deg, var(--hm-accent), #b14ff7); color: #fff; font: 700 15px var(--sans);
  display: grid; place-items: center; box-shadow: inset 0 0 0 1px rgba(255,255,255,.22); }
.search { flex: 1; display: flex; align-items: center; gap: 8px; height: 34px; padding: 0 12px;
  border-radius: 999px; background: rgba(255,255,255,.10); color: rgba(255,255,255,.75); font-size: 14px;
  backdrop-filter: blur(6px); }
.hb-ic { width: 34px; height: 34px; border: 0; border-radius: 50%; background: rgba(255,255,255,.10);
  color: rgba(255,255,255,.85); font-size: 15px; cursor: pointer; display: grid; place-items: center; }
.hb-ic:hover { background: rgba(255,255,255,.18); }
.hb-ic svg { width: 17px; height: 17px; }
.hb-ic.dark { background: var(--panel); color: var(--ink); font-size: 18px; }

.hero-mid { text-align: center; padding: 26px 0 8px; }
.acct-lbl { font: 500 13px var(--sans); color: rgba(255,255,255,.66); letter-spacing: .02em; }
.balance { font: 650 44px/1.15 var(--sans); letter-spacing: -.02em; color: #fff;
  font-variant-numeric: tabular-nums; margin: 2px 0 10px; }
.pill { border: 0; border-radius: 999px; padding: 7px 16px; cursor: pointer;
  background: rgba(255,255,255,.14); color: #fff; font: 600 13px var(--sans); backdrop-filter: blur(6px); }
.pill:hover { background: rgba(255,255,255,.22); }
.wallet-state { margin-top: 10px; font: 500 12.5px var(--sans); color: rgba(255,255,255,.62); }
.wallet-state b { color: #ffd479; font-weight: 600; }

.accounts { margin: 12px 4px 2px; border-radius: var(--r); overflow: hidden;
  background: rgba(255,255,255,.07); backdrop-filter: blur(10px); }
.acct-row { display: flex; align-items: center; gap: 12px; width: 100%; padding: 11px 14px; color: #fff;
  border: 0; background: none; font: inherit; text-align: left; cursor: pointer; }
.acct-row + .acct-row { border-top: 1px solid rgba(255,255,255,.07); }
.acct-row:hover { background: rgba(255,255,255,.06); }
.acct-row.sel { background: color-mix(in srgb, var(--hm-accent) 26%, transparent); }
.acct-row .a-ic { width: 30px; height: 30px; border-radius: 50%; display: grid; place-items: center;
  background: rgba(255,255,255,.13); font: 700 12px var(--mono); }
.acct-row .a-nm { flex: 1; font: 550 14.5px var(--sans); text-transform: capitalize; }
.acct-row .a-amt { font: 600 14.5px var(--sans); font-variant-numeric: tabular-nums; }
.acct-row .a-fiat { font: 400 12px var(--sans); color: rgba(255,255,255,.55); margin-left: 8px; }

.quick { display: flex; justify-content: space-around; padding: 16px 6px 2px; }
.quick button { display: flex; flex-direction: column; align-items: center; gap: 7px; width: 76px;
  border: 0; background: none; color: rgba(255,255,255,.88); font: 500 12.5px var(--sans); cursor: pointer; }
.quick .q-ic { width: 48px; height: 48px; border-radius: 50%; display: grid; place-items: center;
  background: rgba(255,255,255,.13); font-size: 19px; backdrop-filter: blur(8px);
  transition: background .14s ease, transform .12s ease; }
.quick button:hover .q-ic { background: rgba(255,255,255,.22); }
.quick button:active .q-ic { transform: scale(.92); }
.quick button:disabled { opacity: .45; cursor: default; }

/* ── transactions ────────────────────────────────────────────────── */
.txs { padding: 10px 12px 24px; }
.tx-list { border-radius: var(--r); background: var(--panel); overflow: hidden; }
.tx-day { padding: 10px 14px 4px; font: 600 12px var(--sans); color: var(--dim);
  text-transform: uppercase; letter-spacing: .05em; background: var(--panel); }
.tx-day:not(:first-child) { border-top: 1px solid color-mix(in srgb, var(--line) 55%, transparent); padding-top: 12px; }
.tx { display: flex; align-items: center; gap: 12px; width: 100%; padding: 12px 14px; border: 0;
  background: none; color: var(--ink); text-align: left; cursor: pointer; font: inherit; }
.tx:hover { background: var(--panel2); }
.tx + .tx { border-top: 1px solid color-mix(in srgb, var(--line) 40%, transparent); }
.tx .t-av { width: 38px; height: 38px; border-radius: 50%; flex: 0 0 auto; display: grid; place-items: center;
  font: 700 13px var(--mono); color: #fff; }
.tx .t-body { flex: 1; min-width: 0; }
.tx .t-nm { font: 550 14.5px var(--sans); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; display: block; }
.tx .t-sub { font: 400 12.5px var(--sans); color: var(--dim); }
.tx .t-amt { font: 600 14.5px var(--sans); font-variant-numeric: tabular-nums; white-space: nowrap; }
.tx .t-amt.in { color: var(--ok); }
.see-all { display: block; margin: 10px auto 0; border: 0; background: none; color: var(--dim);
  font: 550 13px var(--sans); cursor: pointer; }
.see-all:hover { color: var(--ink); }
.empty { padding: 26px 18px; text-align: center; color: var(--dim); font-size: 13.5px; }

/* Q line — one quiet observation, dismissable, never a feed */
#q-line { display: flex; align-items: center; gap: 10px; margin-top: 12px; padding: 11px 14px;
  border-radius: var(--r); background: color-mix(in srgb, var(--hm-accent) 12%, var(--panel));
  border: 1px solid color-mix(in srgb, var(--hm-accent) 25%, transparent); }
#q-line[hidden] { display: none; }
#q-line .q-orb { width: 22px; height: 22px; border-radius: 50%; flex: 0 0 auto;
  background: radial-gradient(circle at 34% 30%, #fff5, transparent 55%), var(--hm-accent); }
#q-line .q-txt { flex: 1; font: 450 13px/1.4 var(--sans); color: var(--ink); cursor: pointer; }
#q-line .q-x { border: 0; background: none; color: var(--dim); font-size: 15px; cursor: pointer; }

/* ── exchange ────────────────────────────────────────────────────── */
.ex-rows { position: relative; margin: 10px 18px 0; display: flex; flex-direction: column; gap: 6px; }
.ex-row { display: flex; align-items: center; gap: 12px; padding: 15px 16px; border: 1px solid var(--line);
  border-radius: var(--r); background: var(--panel); color: var(--ink); font: inherit; cursor: pointer; text-align: left; }
.ex-row:hover { border-color: var(--hm-accent); }
.ex-row .ex-side { font: 600 12px var(--sans); color: var(--dim); text-transform: uppercase; letter-spacing: .06em; width: 38px; }
.ex-row .ex-cur { font: 650 18px var(--sans); }
.ex-row .ex-bal { margin-left: auto; font: 450 12.5px var(--sans); color: var(--dim); font-variant-numeric: tabular-nums; }
.ex-swap { position: absolute; right: 26px; top: 50%; transform: translateY(-50%); z-index: 2;
  width: 38px; height: 38px; border-radius: 50%; border: 0; cursor: pointer;
  background: var(--hm-accent); color: #fff; font: 700 15px var(--sans); box-shadow: 0 4px 14px rgba(0,0,0,.4); }
.ex-swap:active { transform: translateY(-50%) scale(.92); }
.ex-amt { text-align: center; font: 650 52px/1.1 var(--sans); font-variant-numeric: tabular-nums;
  letter-spacing: -.02em; padding: 26px 18px 4px; min-height: 88px; }
.ex-amt.zero { color: var(--dim); }
.ex-rate { text-align: center; font: 500 13px var(--sans); color: var(--dim); min-height: 20px;
  font-variant-numeric: tabular-nums; }
.ex-rate.bad { color: #ffd479; }
.ex-cta { margin: 10px 18px calc(16px + env(safe-area-inset-bottom)); width: auto; display: block; }

/* the ONE keypad */
.hm-keypad { display: grid; grid-template-columns: repeat(3, 1fr); gap: 4px; padding: 8px 26px; }
.hm-keypad button { height: 58px; border: 0; border-radius: 14px; background: none; color: var(--ink);
  font: 500 24px var(--sans); cursor: pointer; transition: background .1s ease; font-variant-numeric: tabular-nums; }
.hm-keypad button:hover { background: var(--panel); }
.hm-keypad button:active { background: var(--panel2); }

/* ── cards ───────────────────────────────────────────────────────── */
.card-rail { display: flex; gap: 14px; padding: 14px 18px 6px; overflow-x: auto;
  scroll-snap-type: x mandatory; scrollbar-width: none; }
.card-rail::-webkit-scrollbar { display: none; }
.card-face { position: relative; flex: 0 0 min(86%, 372px); aspect-ratio: 1.586; border-radius: 18px;
  overflow: hidden; scroll-snap-align: center; cursor: pointer; background: #0a0a0d;
  box-shadow: 0 18px 40px rgba(0,0,0,.5), inset 0 0 0 1px rgba(255,255,255,.08);
  transition: transform .25s cubic-bezier(.2,.85,.25,1); transform-style: preserve-3d; }
.card-face canvas { position: absolute; inset: 0; width: 100%; height: 100%; }
.card-face.frozen::after { content: "FROZEN"; position: absolute; inset: 0; display: grid; place-items: center;
  background: rgba(8,12,20,.55); backdrop-filter: blur(5px) saturate(.4);
  color: #cfe0ff; font: 700 15px/1 var(--sans); letter-spacing: .35em; }
.card-face .cf-tag { position: absolute; left: 12px; top: 10px; padding: 3px 9px; border-radius: 999px;
  background: rgba(0,0,0,.45); color: #fff; font: 650 10px var(--sans); letter-spacing: .12em; backdrop-filter: blur(4px); }
.card-meta { padding: 12px 18px 24px; }
.cm-row { display: flex; align-items: center; justify-content: space-between; padding: 11px 2px;
  border-bottom: 1px solid color-mix(in srgb, var(--line) 55%, transparent); font-size: 14px; }
.cm-row .k { color: var(--dim); }
.cm-row .v { font-family: var(--mono); font-size: 12.5px; max-width: 60%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.cm-actions { display: flex; gap: 10px; padding-top: 14px; }
.cm-actions button { flex: 1; }
.cm-note { font: 450 12.5px/1.5 var(--sans); color: var(--dim); padding-top: 10px; }
.cm-archive { margin-top: 14px; }
.cm-archive h3 { font: 600 12px var(--sans); color: var(--dim); text-transform: uppercase; letter-spacing: .06em; margin: 0 0 8px; }
.arch-row { display: flex; align-items: center; gap: 10px; padding: 8px 0; color: var(--dim); font: 450 12.5px var(--mono); }
.arch-row canvas { width: 52px; aspect-ratio: 1.586; border-radius: 5px; }

/* mint type toggle */
.type-row { display: flex; gap: 10px; margin-top: 8px; }
.type-opt { flex: 1; padding: 12px; border: 2px solid var(--line); border-radius: 14px; background: var(--panel);
  color: var(--ink); cursor: pointer; text-align: left; font: inherit; }
.type-opt.sel { border-color: var(--hm-accent); }
.type-opt b { display: block; font: 650 14px var(--sans); }
.type-opt span { font: 450 11.5px/1.35 var(--sans); color: var(--dim); }

/* ── payments (recipient-first) ──────────────────────────────────── */
.rcp-search { padding: 6px 18px 2px; position: relative; }
.rcp-search input { width: 100%; min-height: 44px; padding: 0 13px; color: var(--ink);
  background: var(--panel); border: 1px solid var(--line); border-radius: 12px; font: 400 15px var(--sans); }
.rcp-search input:focus { outline: none; border-color: var(--hm-accent); }
.rcp-hint { position: absolute; right: 30px; top: 50%; transform: translateY(-50%);
  font: 600 12px var(--sans); color: var(--ok); pointer-events: none; }
.rcp-list { padding: 10px 18px 4px; }
.rcp-sec { font: 600 12px var(--sans); color: var(--dim); text-transform: uppercase; letter-spacing: .05em; padding: 10px 2px 6px; }
.rcp { display: flex; align-items: center; gap: 12px; width: 100%; padding: 11px 12px; border: 0;
  border-radius: 14px; background: none; color: var(--ink); text-align: left; cursor: pointer; font: inherit; }
.rcp:hover { background: var(--panel); }
.rcp .r-av { width: 40px; height: 40px; border-radius: 50%; flex: 0 0 auto; display: grid; place-items: center;
  color: #fff; font: 700 14px var(--sans); }
.rcp .r-body { flex: 1; min-width: 0; }
.rcp .r-nm { font: 550 14.5px var(--sans); display: block; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.rcp .r-sub { font: 400 12px var(--mono); color: var(--dim); }
.rcp.checked .r-av { outline: 3px solid var(--hm-accent); outline-offset: 1px; }
.cta-sm.ghosted { background: var(--panel); color: var(--ink); }
.pay-links { padding: 4px 18px 24px; }
.pl-row { display: flex; align-items: center; gap: 12px; padding: 12px 14px; margin-bottom: 10px;
  border-radius: var(--r); background: var(--panel); }
.pl-row .t-body { flex: 1; min-width: 0; }
.pl-row button { flex: 0 0 auto; }

/* send/confirm sheet bits */
.cf-row { display: flex; justify-content: space-between; align-items: center; padding: 10px 2px;
  border-bottom: 1px solid color-mix(in srgb, var(--line) 50%, transparent); font-size: 14px; }
.cf-row .k { color: var(--dim); }
.cf-row .v { font-variant-numeric: tabular-nums; }
.sheet-amt { text-align: center; font: 650 44px/1.1 var(--sans); font-variant-numeric: tabular-nums; padding: 14px 0 2px; }
.sheet-amt.zero { color: var(--dim); }
.sheet-sub { text-align: center; color: var(--dim); font-size: 13px; min-height: 18px; }

/* ── stats ───────────────────────────────────────────────────────── */
#stats-body { padding: 8px 18px 24px; }
.bars { display: flex; align-items: flex-end; gap: 8px; height: 140px; padding: 14px;
  border-radius: var(--r); background: var(--panel); }
.bar { flex: 1; display: flex; flex-direction: column; justify-content: flex-end; align-items: center; gap: 6px; height: 100%; }
.bar i { display: block; width: 100%; border-radius: 6px 6px 2px 2px;
  background: linear-gradient(180deg, var(--hm-accent), color-mix(in srgb, var(--hm-accent) 45%, transparent)); }
.bar span { font: 500 10.5px var(--sans); color: var(--dim); }
.stat-note { padding: 12px 4px; color: var(--dim); font-size: 12.5px; }

/* ── tab bar ─────────────────────────────────────────────────────── */
#tabs { position: absolute; left: 0; right: 0; bottom: 0; height: calc(64px + env(safe-area-inset-bottom));
  padding-bottom: env(safe-area-inset-bottom); display: flex;
  background: color-mix(in srgb, var(--bg) 88%, transparent); backdrop-filter: blur(16px) saturate(1.4);
  border-top: 1px solid color-mix(in srgb, var(--line) 70%, transparent); }
#tabs button { flex: 1; border: 0; background: none; color: var(--dim); cursor: pointer;
  display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px;
  font: 550 11px var(--sans); transition: color .14s ease; }
#tabs button .t-ic { font-size: 18px; line-height: 1; }
#tabs button.on { color: var(--ink); }
#tabs button:hover { color: var(--ink); }

/* ── bottom sheet ────────────────────────────────────────────────── */
#sheet { position: absolute; inset: 0; z-index: 40; background: rgba(2,4,10,.55); backdrop-filter: blur(3px);
  display: flex; align-items: flex-end; justify-content: center; }
#sheet[hidden], #onboard[hidden] { display: none; }
#sheet-card { width: 100%; max-width: var(--col); max-height: 86%; overflow-y: auto;
  border-radius: 22px 22px 0 0; background: var(--panel); padding: 10px 18px calc(18px + env(safe-area-inset-bottom));
  animation: sheet-in .28s cubic-bezier(.2,.85,.25,1); }
@keyframes sheet-in { from { transform: translateY(40px); opacity: .4; } to { transform: none; opacity: 1; } }
#sheet-card h2 { margin: 12px 0 4px; font: 700 19px var(--sans); }
#sheet-card .grab { width: 42px; height: 4px; border-radius: 2px; background: var(--line); margin: 4px auto 2px; }
#sheet-card label { display: block; margin: 14px 0 5px; font: 600 12px var(--sans); color: var(--dim);
  text-transform: uppercase; letter-spacing: .06em; }
#sheet-card input, #sheet-card select { width: 100%; min-height: 44px; padding: 0 13px; color: var(--ink);
  background: var(--bg); border: 1px solid var(--line); border-radius: 12px; font: 400 15px var(--sans); }
#sheet-card input:focus, #sheet-card select:focus { outline: none; border-color: var(--hm-accent); }
.sheet-note { color: var(--dim); font-size: 12.5px; margin-top: 10px; }
.sheet-note.test { color: #ffd479; }
.addr { font: 500 12.5px var(--mono); word-break: break-all; padding: 12px; border-radius: 12px;
  background: var(--bg); border: 1px dashed var(--line); margin-top: 6px; }

button.cta, .cta-sm { border: 0; border-radius: 999px; cursor: pointer; background: var(--hm-accent); color: #fff;
  font: 650 15px var(--sans); transition: filter .12s ease, transform .1s ease; }
button.cta { width: 100%; min-height: 48px; margin-top: 18px; }
.cta-sm { padding: 8px 15px; font-size: 13px; }
button.cta:hover, .cta-sm:hover { filter: brightness(1.12); }
button.cta:active { transform: scale(.985); }
button.cta:disabled { opacity: .5; filter: none; cursor: default; }
button.ghost { border: 1px solid var(--line); border-radius: 999px; background: none; color: var(--ink);
  min-height: 44px; font: 550 14px var(--sans); cursor: pointer; }
button.ghost:hover { border-color: var(--hm-accent); }

/* edition picker (mint sheet + onboarding) */
.editions { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 8px; }
.ed { position: relative; border: 2px solid transparent; border-radius: 14px; overflow: hidden; cursor: pointer;
  aspect-ratio: 1.586; background: #0a0a0d; }
.ed canvas { position: absolute; inset: 0; width: 100%; height: 100%; }
.ed.sel { border-color: var(--hm-accent); }
.ed .ed-nm { position: absolute; left: 8px; bottom: 6px; color: #fff; font: 650 11.5px var(--sans);
  text-shadow: 0 1px 4px rgba(0,0,0,.7); }
.ed.locked { opacity: .55; }
.ed .ed-lock { position: absolute; right: 8px; top: 6px; font-size: 12px; color: #fff; }

/* ── onboarding ──────────────────────────────────────────────────── */
#onboard { position: absolute; inset: 0; z-index: 50; display: flex; flex-direction: column;
  background:
    radial-gradient(150% 100% at 50% -30%, color-mix(in srgb, var(--hm-accent) 55%, #241a7a) 0%, rgba(16,12,44,.9) 50%, #0c0d12 100%); }
.ob { flex: 1; display: flex; flex-direction: column; justify-content: center; align-items: center;
  padding: 28px; text-align: center; color: #fff; animation: sheet-in .3s ease; }
.ob h1 { font: 700 30px/1.2 var(--sans); letter-spacing: -.02em; margin: 0 0 10px; max-width: 20ch; }
.ob p { color: rgba(255,255,255,.72); font-size: 15px; max-width: 34ch; margin: 0 0 8px; }
.ob .ob-badges { display: flex; gap: 8px; margin: 14px 0 4px; flex-wrap: wrap; justify-content: center; }
.ob .badge { padding: 6px 12px; border-radius: 999px; background: rgba(255,255,255,.12); font: 550 12px var(--sans); }
.ob .badge.ok { background: color-mix(in srgb, var(--ok) 28%, transparent); }
.ob input { width: min(100%, 320px); min-height: 46px; margin-top: 14px; padding: 0 14px; text-align: center;
  color: #fff; background: rgba(255,255,255,.10); border: 1px solid rgba(255,255,255,.25); border-radius: 12px;
  font: 500 16px var(--sans); }
.ob input:focus { outline: none; border-color: #fff; }
.ob .editions { width: min(100%, 360px); }
.ob-foot { padding: 0 28px calc(26px + env(safe-area-inset-bottom)); }
.ob-foot .cta { max-width: 360px; margin: 0 auto 10px; display: block; }
.ob-skip { display: block; margin: 0 auto; border: 0; background: none; color: rgba(255,255,255,.6);
  font: 500 13px var(--sans); cursor: pointer; }
.ob-kappa { font: 400 10.5px var(--mono); color: rgba(255,255,255,.45); word-break: break-all; max-width: 40ch; margin-top: 12px; }

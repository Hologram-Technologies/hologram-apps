<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <title>Holo Money</title>
    <meta name="description" content="Holo Money — the neobank surface of Hologram OS. Real balances over the sovereign wallet (WDK), money as κ-links (Holo Pay), virtual cards as κ-objects with generative faces. One column: phone, and the desktop right carriage, are the same app." />

    <link rel="stylesheet" href="../../_shared/holo-theme.css" />
    <link rel="stylesheet" href="../../_shared/holo-mobile.css" />
    <!-- holo-theme.js boots theming + Holo UX (one canonical wire, Law L2) -->
    <script src="../../_shared/holo-theme.js"></script>
    <link rel="stylesheet" href="./style.css" />
  <link rel="icon" href="./icon.svg">
<script>try{if("serviceWorker" in navigator)navigator.serviceWorker.register(new URL("../../root-sw.js",location.href),{type:"module"}).catch(function(){navigator.serviceWorker.register(new URL("../../root-sw-classic.js",location.href)).catch(function(){})})}catch(e){}</script>
</head>
  <body data-holo-boot="off">
    <div id="app" aria-busy="true">
      <!-- ── HOME ─────────────────────────────────────────────────── -->
      <section id="scr-home" class="screen on">
        <div class="hero">
          <header class="hero-bar">
            <button id="avatar" class="avatar" title="You" aria-label="Identity"></button>
            <div class="search" role="search"><span class="s-ic">⌕</span><span class="s-ph">Search</span></div>
            <button id="cards-ic" class="hb-ic" title="Cards" aria-label="Cards"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="6" width="18" height="13" rx="2.5"/><path d="M3 10.5h18"/></svg></button>
            <button id="stats-ic" class="hb-ic" title="Analytics" aria-label="Analytics">▦</button>
          </header>
          <div class="hero-mid">
            <div class="acct-lbl" id="acct-lbl">Personal · <span id="fiat-unit">USD</span></div>
            <div id="balance" class="balance">$0</div>
            <button id="accounts-pill" class="pill" aria-expanded="false">Accounts</button>
            <div id="wallet-state" class="wallet-state" hidden></div>
          </div>
          <div id="accounts" class="accounts" hidden></div>
          <nav class="quick" aria-label="Quick actions">
            <button data-act="add"><span class="q-ic">+</span><span>Add money</span></button>
            <button data-act="move"><span class="q-ic">⇄</span><span>Move</span></button>
            <button data-act="details"><span class="q-ic">≡</span><span>Details</span></button>
            <button data-act="more"><span class="q-ic">···</span><span>More</span></button>
          </nav>
        </div>
        <div class="txs">
          <div id="tx-list" class="tx-list"><div class="empty">Loading activity…</div></div>
          <button id="tx-all" class="see-all" hidden>See all</button>
          <div id="q-line" hidden></div>
        </div>
      </section>

      <!-- ── EXCHANGE (Move) — no tab; back returns home ──────────── -->
      <section id="scr-exchange" class="screen">
        <header class="scr-hd"><button class="back" data-back aria-label="Back">‹</button><h1>Exchange</h1><span class="hd-sp"></span></header>
        <div class="ex-rows">
          <button class="ex-row" id="ex-from">
            <span class="ex-side">Sell</span>
            <span class="ex-cur" id="ex-from-cur">ETH</span>
            <span class="ex-bal" id="ex-from-bal"></span>
          </button>
          <button class="ex-swap" id="ex-swap" title="Swap direction" aria-label="Swap direction">↓↑</button>
          <button class="ex-row" id="ex-to">
            <span class="ex-side">Buy</span>
            <span class="ex-cur" id="ex-to-cur">SOL</span>
            <span class="ex-bal" id="ex-to-bal"></span>
          </button>
        </div>
        <div class="ex-amt" id="ex-amt">0</div>
        <div class="ex-rate" id="ex-rate"></div>
        <div id="ex-keypad"></div>
        <button class="cta ex-cta" id="ex-go" disabled>Sell</button>
      </section>

      <!-- ── CARDS ────────────────────────────────────────────────── -->
      <section id="scr-cards" class="screen">
        <header class="scr-hd"><h1>Cards</h1><button id="card-add" class="cta-sm">＋ Get card</button></header>
        <div id="card-rail" class="card-rail" tabindex="0"></div>
        <div id="card-meta" class="card-meta"></div>
      </section>

      <!-- ── PAYMENTS — recipient-first ───────────────────────────── -->
      <section id="scr-payments" class="screen">
        <header class="scr-hd"><h1>Payments</h1>
          <span class="hd-actions">
            <button id="split-btn" class="cta-sm ghosted">Split a bill</button>
            <button id="rcp-add" class="hb-ic dark" title="New recipient" aria-label="New recipient">＋</button>
          </span>
        </header>
        <div class="rcp-search"><input id="rcp-filter" placeholder="Name, three words, κ, or address" autocomplete="off" /><span id="rcp-hint" class="rcp-hint"></span></div>
        <div id="rcp-list" class="rcp-list"></div>
        <div id="pay-links" class="pay-links"></div>
      </section>

      <!-- ── STATS ────────────────────────────────────────────────── -->
      <section id="scr-stats" class="screen">
        <header class="scr-hd"><h1>Analytics</h1></header>
        <div id="stats-body"><div class="empty">Spending view builds from your on-chain history.</div></div>
      </section>

      <!-- ── tab bar ──────────────────────────────────────────────── -->
      <nav id="tabs" aria-label="Sections">
        <button data-scr="home" class="on"><span class="t-ic">⌂</span>Home</button>
        <button data-scr="cards"><span class="t-ic">▭</span>Cards</button>
        <button data-scr="payments"><span class="t-ic">⇄</span>Payments</button>
        <button data-scr="stats"><span class="t-ic">◔</span>Stats</button>
      </nav>

      <!-- ── bottom sheet (one, repopulated) ──────────────────────── -->
      <div id="sheet" hidden><div id="sheet-card" role="dialog" aria-modal="true"></div></div>

      <!-- ── onboarding overlay ───────────────────────────────────── -->
      <div id="onboard" hidden></div>
    </div>
    <script type="module" src="./app.mjs"></script>
  </body>
</html>

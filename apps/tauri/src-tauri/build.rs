// Canonical Tauri v2 build script — verbatim from the spec (tauri-apps/tauri). Generates the
// context, embeds the config, and wires the plugins' build steps. Do not hand-edit.
fn main() {
    tauri_build::build()
}
